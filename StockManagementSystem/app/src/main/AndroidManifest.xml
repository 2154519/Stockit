<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stockmanagementsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StockManagementSystem">
        <activity android:name=".GenerateReportScreen1"></activity>
        <activity android:name=".TakeInventoryScreen" />
        <activity android:name=".HomeScreen" />
        <activity android:name=".RegisterScreen2" />
        <activity android:name=".RegisterScreen1" />
        <activity android:name=".SignInScreen" />
        <activity android:name=".StartScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>